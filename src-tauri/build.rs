fn main() {
    // The daemon is a standalone CLI binary, not a Tauri app.
    // `npx tauri build` requires a separate Tauri Rust entry point.
    // For the daemon build, use: cargo build --release
}
